%header
  %nav#top-bar
    %section
      = link_to (render "shared/logo"), root_path
      %article
        - if user_signed_in?
          = link_to 'Events', events_path
          = link_to 'Challenges', challenges_path
          = link_to 'Datasets', data_sets_path
          = link_to 'Projects', projects_path
          = link_to 'Teams', manage_account_path # Change to correct path
          = link_to 'Help', root_path # Change to correct path
        - else
          = link_to 'Events', events_path
          = link_to 'Challenges', challenges_path
          = link_to 'Datasets', data_sets_path
          = link_to 'Projects', projects_path
          = link_to 'Help', root_path # Change to correct path
    = render "shared/session_login_links"
